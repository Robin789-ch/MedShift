[project]
name = "medshift-optimizer"
version = "0.2.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.128,<1",
    "medshift-contracts",
    "ortools==9.15.6755",
    "uvicorn>=0.40,<1",
]

[build-system]
requires = ["uv-build>=0.11.21,<0.12"]
build-backend = "uv_build"
