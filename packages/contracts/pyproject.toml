[project]
name = "medshift-contracts"
version = "0.2.0"
requires-python = ">=3.14"
dependencies = [
    "pydantic>=2.12,<3",
]

[build-system]
requires = ["uv-build>=0.11.21,<0.12"]
build-backend = "uv_build"
